
/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/creatisala',
  assetPrefix: '/creatisala/',
  output: 'export',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
